import { defineFormat } from "./define-format";

export const tiffFormat = defineFormat("tiff", "images-video", 4,
    { name: "TIFF image", title: "Open TIFF Images Online", description: "Browse common tiled, compressed and multi-page TIFF images locally.", introduction: "TIFF is a flexible raster container used in scanning, publishing, microscopy and geospatial work. Anyfile reads its directory structure and decodes common strips, tiles and pages with the dedicated raster viewer.", canShow: ["Common TIFF compression variants", "Tiled and stripped images", "Multiple image directories or pages"], limitations: ["Uncommon private tags are not interpreted", "Unknown compression schemes may allow metadata inspection but not pixels"], faq: [{ question: "Does TIFF support depend on its compression?", answer: "Yes. TIFF is a container; an uncommon compression method can prevent pixel decoding even when the directory metadata is readable." }] },
    { name: "TIFF 图片", title: "在线打开 TIFF 图片", description: "在本地浏览常见分块、压缩与多页 TIFF 图片。", introduction: "TIFF 是扫描、出版、显微和地理空间工作中常见的灵活栅格容器。Anyfile 读取其目录结构，并用专用栅格查看器解码常见条带、分块和页面。", canShow: ["常见 TIFF 压缩变体", "分块与条带图像", "多个图像目录或页面"], limitations: ["不会解释少见的私有标签", "未知压缩方式可能只能检查元数据，无法解码像素"], faq: [{ question: "TIFF 支持是否取决于压缩方式？", answer: "是。TIFF 是容器；即使目录元数据可读，少见压缩方式仍可能导致像素无法解码。" }] }, { possibleLevels: [1, 3, 4] }, undefined, ["tif", "tf8", "btf", "btiff", "ptif", "ptiff"]);
