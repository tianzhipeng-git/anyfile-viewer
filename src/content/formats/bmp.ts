import { defineFormat } from "./define-format";

export const bmpFormat = defineFormat("bmp", "images-video", 4,
  { name: "BMP image", title: "Open BMP Images Online", description: "View Windows bitmap pixels locally with native decoding.", introduction: "BMP is a family of device-independent bitmap layouts with headers describing dimensions, bit depth, palettes and compression. Anyfile asks the browser to decode the selected bitmap and displays the verified result.", canShow: ["Common RGB and palette-based bitmaps", "Browser-supported bit depths and compression variants"], limitations: ["Rare legacy headers or compression modes may not decode", "The viewer does not expose every bitmap header field"], faq: [{ question: "Are BMP files always uncompressed?", answer: "No. Many are stored as direct pixel rows, but the format also defines palette and run-length compressed variants." }] },
  { name: "BMP 图片", title: "在线打开 BMP 图片", description: "通过原生解码在本地查看 Windows 位图像素。", introduction: "BMP 是设备无关位图布局家族，文件头描述尺寸、位深、调色板与压缩。Anyfile 让浏览器解码所选位图并显示经验证的结果。", canShow: ["常见 RGB 与调色板位图", "浏览器支持的位深与压缩变体"], limitations: ["少见旧式文件头或压缩方式可能无法解码", "查看器不会展示所有位图文件头字段"], faq: [{ question: "BMP 文件总是不压缩吗？", answer: "不是。许多 BMP 直接保存像素行，但格式也定义调色板与游程压缩变体。" }] }, {}, undefined, ["dib"]);
