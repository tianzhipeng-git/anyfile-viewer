import { defineFormat } from "./define-format";

export const apngFormat = defineFormat("apng", "images-video", 4,
  { name: "Animated PNG", title: "Open APNG Images Online", description: "View animated PNG frames locally with native image decoding.", introduction: "APNG extends PNG with animation control and frame data while retaining PNG-compatible chunks. Anyfile verifies that the browser can decode the selected image before displaying its animation.", canShow: ["Animated frames with alpha transparency", "The browser-decoded image at its intrinsic size"], limitations: ["Frame timing and blending depend on the browser decoder", "The viewer does not extract or edit individual frames"], faq: [{ question: "What happens if APNG is opened by an older PNG-only tool?", answer: "The format is designed so a PNG-only decoder can often show the default image while ignoring animation chunks." }] },
  { name: "动态 PNG", title: "在线打开 APNG 图片", description: "通过浏览器原生图片解码在本地查看动态 PNG 帧。", introduction: "APNG 在兼容 PNG chunk 的基础上增加动画控制与帧数据。Anyfile 会确认当前浏览器能解码所选图片，再展示其中动画。", canShow: ["带 Alpha 透明度的动画帧", "浏览器解码后的原始尺寸图片"], limitations: ["帧时序与混合效果取决于浏览器解码器", "查看器不提取或编辑单独帧"], faq: [{ question: "旧式只支持 PNG 的工具打开 APNG 会怎样？", answer: "该格式通常允许 PNG-only 解码器显示默认图片，并忽略动画 chunk。" }] });
