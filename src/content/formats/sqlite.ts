import { defineFormat } from "./define-format";

export const sqliteFormat = defineFormat("sqlite", "code-data", 5,
    { name: "SQLite database", title: "Open SQLite Databases Online", description: "Browse SQLite tables, schemas and rows locally in read-only mode.", introduction: "SQLite stores a relational database in one file. Anyfile loads that file into sql.js WebAssembly, enumerates its schema and lets you inspect tables in paged, read-only views.", canShow: ["Tables, columns and declared types", "Paged table rows", "Database schema needed for navigation"], limitations: ["The viewer does not run arbitrary user SQL or write changes", "Encrypted, corrupt or unsupported SQLite extensions may not open"], faq: [{ question: "Can opening a SQLite file change the database?", answer: "No. The browser session is used for read-only inspection and Anyfile does not save modifications to the selected file." }] },
    { name: "SQLite 数据库", title: "在线打开 SQLite 数据库", description: "以只读方式在本地浏览 SQLite 表、schema 与记录。", introduction: "SQLite 把关系数据库保存在单个文件中。Anyfile 使用 sql.js WebAssembly 加载该文件，枚举 schema，并以只读分页视图检查数据表。", canShow: ["数据表、列与声明类型", "分页表记录", "导航所需的数据库 schema"], limitations: ["查看器不运行任意用户 SQL，也不写入修改", "加密、损坏或依赖不支持扩展的 SQLite 可能无法打开"], faq: [{ question: "打开 SQLite 文件会改变数据库吗？", answer: "不会。浏览器会话只用于只读检查，Anyfile 不会把修改写回所选文件。" }] }, {}, undefined, ["sqlite3", "db"]);
