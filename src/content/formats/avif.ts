import { defineFormat } from "./define-format";

export const avifFormat = defineFormat("avif", "images-video", 4,
  { name: "AVIF image", title: "Open AVIF Images Online", description: "View browser-decodable AVIF still images and sequences locally.", introduction: "AVIF stores AV1-coded image items in an ISO Base Media container and can include alpha, sequences and high-bit-depth data. Anyfile uses the current browser's real decoder for the selected file.", canShow: ["Supported AV1-coded primary images", "Browser-supported alpha and image sequences"], limitations: ["HDR, color management and uncommon item layouts vary by browser", "Unsupported AV1 profiles or very large images may fail"], faq: [{ question: "Why does AVIF support vary between browsers?", answer: "The container can use different AV1 profiles, bit depths and item relationships, and browser decoders do not support every combination." }] },
  { name: "AVIF 图片", title: "在线打开 AVIF 图片", description: "在本地查看浏览器可解码的 AVIF 静态图片与序列。", introduction: "AVIF 在 ISO Base Media 容器中保存 AV1 编码图像项，可包含 Alpha、序列与高位深数据。Anyfile 使用当前浏览器的真实解码器打开所选文件。", canShow: ["受支持 AV1 编码的主图片", "浏览器支持的 Alpha 与图像序列"], limitations: ["HDR、色彩管理与少见 item 布局随浏览器而异", "不支持的 AV1 profile 或超大图片可能失败"], faq: [{ question: "为什么不同浏览器的 AVIF 支持不同？", answer: "容器可使用不同 AV1 profile、位深与 item 关系，而浏览器解码器并不覆盖所有组合。" }] });
