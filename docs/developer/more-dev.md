我想为我们项目增加更多开发者格式的支持. 
在如下这些编程语言中, 有哪些除了语言代码文件以外的格式的文件, 是常见但是不好"打开"的, 比如jar, pkl这类的

Python、JavaScript、TypeScript、Java、C++、C#、SQL、Go、Rust、PHP

- 包括不限于:
- 编译产物 / 字节码
- 序列化数据
- 数据库文件
- 二进制对象 / 静态库 / 动态库
- 调试符号 / 中间产物
- 依赖包 / 发布包
- 缓存文件

这里有一些能做到`2 代表性预览`或`3 主要内容`级别的支持, 大部分只能做到`1 检查`;
我们要制定一下计划和方案.